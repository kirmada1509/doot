# Caller-First Submission Script (Under 3 Minutes)

Use a healthy fixture stack with the separate authorized CALL-E rehearsal configured. Record the real-call section only after the [one-call gate](submission-status.md) passes. Never call fixture run IDs CALL-E evidence. Doot is a coordination demo, not emergency dispatch or a real provider booking.

## 0:00-0:35 | Dial And Speak

Open `/`. Point out **browser call, not cellular**. Select Shelter, dial `4040` using the keypad, and start the call. Doot speaks the AI disclosure and recording/deletion notice; agree and allow the microphone. Say “I need a wheelchair-accessible shelter tonight.” For a deterministic room demo, click **Use fixture spoken request** and say aloud that browser TTS/STT are in fixture mode.

## 0:35-1:20 | Two Fixture Options

The browser stays connected while Lotus and Ashraya produce two **fixture** holds. Open `/ops` and point to the source labels. These are synthetic inventory options, not telephone conversations.

## 1:20-1:55 | One Choice And Release

Return to `/`. Compare the two temporary options and expiries; keyboard-focus one specific option and press Enter. Show the selected demo reference committed. Back in `/ops`, show the unchosen provider’s fixture release command and terminal result. There is no automatic backup selection or caller PSTN callback.

## 1:55-2:40 | Real CALL-E Proof And Boundary

Open **Proof and audit** and, with ownership confirmation, place the separate CALL-E rehearsal to your personal phone. Answer it and repeat the demo code. Show the authenticated CALL-E call ID, terminal status, structured result, and audited **CALL-E transcript** excerpt. State explicitly: “This was a real CALL-E phone call to my authorized number; the two provider holds and release shown earlier are fixtures.” Close: “Doot does not reserve a real bed or dispatch emergencies.” Trim idle polling from the final recording; keep the published video under three minutes.
